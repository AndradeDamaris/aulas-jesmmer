int main(){
    int base, altura, area;
    cout << "\n Insira a Base: ";
    cin >> base;
    cout << "\n insira a Altura: ";
    cin >> altura;
    area = ((base*altura)/2);
    cout << "\n a Área é: " << area;
    return 0
}